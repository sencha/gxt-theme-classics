package com.sencha.gxt.ui.client;


public class GwtTestGXT extends UITestCase {

  public void testInit() {
    //These only make sense to run on Firefox running on Windows, or HtmlUnit, which fakes both
    //    assertTrue(GXT.isWindows());
    //    assertTrue(GXT.isGecko());
    //    assertTrue(GXT.isGecko1_9());
  }

}
